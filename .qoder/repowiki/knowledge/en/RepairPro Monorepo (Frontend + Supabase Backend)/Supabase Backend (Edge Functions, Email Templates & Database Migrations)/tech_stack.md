Deno runtime with native `Deno.serve` for Edge Functions; React Email (`@react-email/components` + JSX via per-function `deno.json` compilerOptions) for HTML rendering; `@supabase/supabase-js` v2 service-role client for DB access; Lovable email SDK (`@lovable.dev/email-js`) for auth emails and webhook signature verification; Resend connector gateway (`connector-gateway.lovable.dev/resend/emails`) for transactional email delivery; Supabase pgmq-backed queues implemented via custom RPCs (`enqueue_email`, `read_email_batch`, `delete_email`, `move_to_dlq`).